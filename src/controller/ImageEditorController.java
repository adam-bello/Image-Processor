package controller;

/**
 * Represents an interface for the controller for the ImageEditor. The interface has one method
 * used for starting the program.
 */
public interface ImageEditorController {

  /**
   * Starts the Image Editing program.
   */
  void start();
}
